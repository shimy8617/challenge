let urlBackend = "http://localhost:8080/";
let config = {
  urlBackend: urlBackend,
};

export default config;

/*let urlBackend = fetch("http://localhost:8080/api")
  .then((res) => res.json())
  .then((res) => console.log(res));
let config = {
  urlBackend: urlBackend,
};*/
